services:
  bubble-annotator:
    image: bubble-annotator:latest
    container_name: bubble-annotator
    ports:
      - "80:80"
    restart: unless-stopped
    # API Key 通过环境变量注入（可选，也可在前端设置面板填写）
    # environment:
    #   - API_KEY=sk-xxx
